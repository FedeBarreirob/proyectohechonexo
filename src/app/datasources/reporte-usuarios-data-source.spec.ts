import { ReporteUsuariosDataSource } from './reporte-usuarios-data-source';

describe('ReporteUsuariosDataSource', () => {
  it('should create an instance', () => {
    expect(new ReporteUsuariosDataSource()).toBeTruthy();
  });
});
