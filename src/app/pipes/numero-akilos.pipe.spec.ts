import { NumeroAKilosPipe } from './numero-akilos.pipe';

describe('NumeroAKilosPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroAKilosPipe();
    expect(pipe).toBeTruthy();
  });
});
