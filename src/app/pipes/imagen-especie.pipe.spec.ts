import { ImagenEspeciePipe } from './imagen-especie.pipe';

describe('ImagenEspeciePipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenEspeciePipe();
    expect(pipe).toBeTruthy();
  });
});
