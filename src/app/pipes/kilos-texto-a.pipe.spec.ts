import { KilosTextoAPipe } from './kilos-texto-a.pipe';

describe('KilosTextoAPipe', () => {
  it('create an instance', () => {
    const pipe = new KilosTextoAPipe();
    expect(pipe).toBeTruthy();
  });
});
